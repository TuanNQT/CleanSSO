# Clean .NET 9 SSO (Google & Facebook) — Scaffold (Postgres) + React demo

This scaffold demonstrates a Clean Architecture layout for an ASP.NET Core (.NET 9) solution:
- Domain (entities)
- Application (interfaces, services, DTOs)
- Infrastructure (EF Core, repository implementation)
- WebApi (controllers, DI, auth config)
- frontend (React Vite demo)

Replace placeholders (Google/Facebook secrets, PostgreSQL connection) before running.
